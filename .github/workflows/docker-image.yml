name: "Docker Build"

on:
  workflow_dispatch:
    inputs:
      service:
        description: "Quel service builder ?"
        required: true
        type: choice
        options:
          - api
          - worker
          - frontend
      tag:
        description: "Tag de l'image (ex: 1.0.0)"
        required: false
        default: "latest"
permissions:
  contents: read
  packages: write  

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4  

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Connexion au registre GitHub
        uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
        with:
              registry: ghcr.io
              username: ${{ github.actor }}
              password: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Build & Push
        uses: docker/build-push-action@v5
        with:
          context: ./services/${{ inputs.service }}
          file: ./services/${{ inputs.service }}/Dockerfile
          push: true
          tags: |
            ghcr.io/kouamebaudelaire/${{ inputs.service }}:${{ inputs.tag }}
            ghcr.io/kouamebaudelaire/${{ inputs.service }}:latest
            ghcr.io/kouamebaudelaire/worker:latest
            ghcr.io/kouamebaudelaire/frontend:latest
      - id: normalise_image_name
        run: |
          cleaned=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          COMMIT_SHA=$(echo $GITHUB_SHA | cut -c1-7)
          echo "image_name=ghcr.io/$cleaned/odoo:$COMMIT_SHA" >> "$GITHUB_OUTPUT"
